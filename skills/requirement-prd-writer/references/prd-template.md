# 需求说明模板

```md
# <需求名>需求说明

## 1. 需求概述

- 核心改动：<一句话说明本次最核心改动>。
- 影响模块：<后台 / Web5 PC / Web5 H5 / 客户端内链解析 / 活动页等>。
- 原型文件：查看禅道附件 `<需求名>原型-vX.X.zip`，附件内包含<后台配置原型、客户端原型、交互流程图、本需求说明等>。
- 页面截图：<本地评审版可放 Markdown 图片；提交禅道时需转成 HTML 图片或写查看禅道附件，不能保留本机路径>。
- 非改动说明：本次不调整<明确不做的旧逻辑>。
- 关联知识库：
  - 主知识库：`<主功能点>需求知识库`
  - 次要知识库：`<次要功能点>需求知识库`（如无则删除本行）
  - 如用户确认不需要知识库：`无，用户确认本次按独立新功能处理`

## 2. 页面截图

| 页面 | 截图/附件 | 说明 |
| --- | --- | --- |
| <页面名称> | <本地评审版：`![页面名称](相对路径.png)`；提交禅道版：转为 HTML `<img>` 或写“查看禅道附件”> | <说明截图对应的改动位置> |

## 3. 功能改动

### 3.1 <功能点/页面/配置入口>

- 修改位置：<后台菜单、页面、入口、路由或组件>。
- 展示规则：<页面展示、配置项文案、组件状态、空态>。
- 交互规则：<点击、跳转、弹窗、按钮、状态切换>。
- 保存/处理规则：<保存哪些业务配置、何时生效、如何处理历史数据>。
- 异常处理：<加载失败、目标不存在、无权限、不可访问等用户可感知异常>。
- 兼容规则：<旧配置读取、编辑、保存、返回兼容>。

### 3.2 <可选：第二个功能点>

- 修改位置：
- 展示规则：
- 交互规则：
- 保存/处理规则：
- 异常处理：
- 兼容规则：

## 4. 规则与边界

- 业务规则：<关键规则 1>。
- 条件组合：
  - `<条件 A>`：<结果 A>。
  - `<条件 B>`：<结果 B>。
- 优先级：<如有多状态、多对象命中顺序，在这里写清>。
- 状态边界：<哪些状态支持，哪些状态不支持，如何兜底>。
- 不处理范围：<再次明确本次不涉及的旧逻辑>。
- 知识库沉淀：<有关联知识库时写“已关联”；用户确认不需要知识库时写“本次按独立新功能处理，后续持续迭代时再补充知识库沉淀”>。
- 风险点：<用户体验、兼容、数据口径、权限边界等风险>。

## 5. 待确认问题

| 问题 | 影响 | 建议处理 |
| --- | --- | --- |
| 暂无 | - | - |

## 6. 版本记录

| 版本 | 日期 | 作者 | 变更说明 |
| --- | --- | --- | --- |
| v1.0 | <YYYY-MM-DD> | Codex | 创建需求说明。 |
```

## 使用说明

- 默认使用精简结构：需求概述、页面截图、功能改动、规则与边界、待确认问题、版本记录。
- 默认不要写 `改动范围`、`验收标准`、`数据与接口` / `接口与数据字段`；只有用户明确要求技术方案时才增加。
- 需求说明必须使用产品/业务语言，不写研发实现细节：不要出现接口名、数据库表/字段、代码变量、枚举名、类名、方法名、缓存方案、SQL、任务脚本、埋点 key 等技术实现内容。
- 可以写用户可感知或业务可配置的“配置项/页面字段/筛选项/状态/按钮/文案”，但不要写保存到哪个技术字段、接口怎么传、后端如何实现。
- `功能改动` 用产品/业务语言描述页面、配置、交互、保存和生效口径；不要拆前端/后端，不要把研发实现细节写进需求说明。
- 如确实需要补充研发口径，应单独建立“技术方案/实现说明”，不要混入需求说明正文。
- 正式 PRD 默认必须关联至少一个需求知识库；如果没有知识库，先与用户确认是否建立知识库，并使用 `$zentao-kb` 或 `$requirement-kb-updater` 生成/更新后再写 PRD。
- 如果用户确认不需要知识库，可以按独立新功能继续写正式 PRD；`需求概述` 的“关联知识库”写 `无，用户确认本次按独立新功能处理`。
- `需求概述` 中必须保留“关联知识库”；有关联知识库时列出主知识库和次要知识库，不要只写本地路径，提交禅道时写知识库名称即可。
- PRD 中建议保留必要表格，方便提交禅道时转成真实 HTML 表格；能用短列表表达的内容不要强行表格化。
- 准备提交禅道的 PRD 不要包含本机路径或临时地址；页面截图可以随需求附件上传，并在提交时将 Markdown 图片转成 HTML `<img>` 标签引用禅道可访问的图片地址；如果无法获取可访问图片地址，则写“查看禅道附件 `<附件名>`”。
- 提交禅道时，Markdown 正文需转为 HTML：标题、列表、表格和图片都要是真实 HTML 结构；图片建议使用 `<img src="..." style="max-width:100%;" />`，不要把 Markdown 图片语法或本机路径直接写入禅道正文。
- 本地最终回复可以列出绝对路径，方便用户打开；但禅道正文不能出现 `/Users/...`、`/home/...`、`C:\...`、`file://`、`localhost`、`127.0.0.1`。
