interface:
  display_name: "禅道需求提交"
  short_description: "将需求文档提交为禅道需求并上传 .md 附件"
  default_prompt: "Use $zentao-requirement-submit to submit the current requirement to ZenTao with project, version, source, assignee, the Markdown requirement document attachment, and verification."
policy:
  allow_implicit_invocation: true
